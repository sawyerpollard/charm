# Minimal app config. No `compose` key, so Dockerfile mode.
# The route host is a full domain (must be under a registered `charm domain`);
# the value is the container port.
[routes]
"demo.charm.test" = 8080
